// Stub: sharedConfigs and nardukPlugin for local development
// The real @narduk-enterprises/eslint-config is a private GitHub Package.
// This stub lets the workspace install when the registry token is unavailable.
export const sharedConfigs = []
export const nardukPlugin = { rules: {} }
